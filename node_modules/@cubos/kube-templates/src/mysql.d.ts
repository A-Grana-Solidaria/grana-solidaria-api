import { Container, ObjectMeta } from "./kubernetes";
interface MySQLSpec {
    version: string;
    cpu: {
        request: string | number;
        limit: string | number;
    };
    memory: string | number;
    rootPassword: string;
    initContainers?: Container[];
}
export declare class MySQL {
    private metadata;
    private spec;
    constructor(metadata: ObjectMeta, spec: MySQLSpec);
    get yaml(): string;
}
export {};
