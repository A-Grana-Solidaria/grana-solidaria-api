import { Container, ObjectMeta } from "./kubernetes";
interface MSSQLSpec {
    version: string;
    productId: "Evaluation" | "Developer" | "Express" | "Web" | "Standard" | "Enterprise" | "EnterpriseCore" | string;
    cpu: {
        request: string | number;
        limit: string | number;
    };
    memory: string | number;
    sysAdminPassword: string;
    initContainers?: Container[];
}
export declare class MSSQL {
    private metadata;
    private spec;
    constructor(metadata: ObjectMeta, spec: MSSQLSpec);
    get yaml(): string;
}
export {};
