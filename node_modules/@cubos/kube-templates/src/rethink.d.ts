import { ObjectMeta } from "./kubernetes";
interface RethinkSpec {
    replicas?: number;
    version: string;
    cpu: {
        request: string | number;
        limit: string | number;
    };
    memory: string | number;
    cacheMb?: number;
    storageClassName?: string;
}
export declare class Rethink {
    private metadata;
    private spec;
    constructor(metadata: ObjectMeta, spec: RethinkSpec);
    get yaml(): string;
}
export {};
