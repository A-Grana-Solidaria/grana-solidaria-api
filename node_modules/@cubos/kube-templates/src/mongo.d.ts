import { BasicObjectMeta, ObjectMeta } from "./kubernetes";
interface MongoSpec {
    version: string;
    cpu: {
        request: string | number;
        limit: string | number;
    };
    memory: string | number;
    auth?: {
        username: string;
        password: string;
    };
    serviceType?: "ExternalName" | "ClusterIP" | "NodePort" | "LoadBalancer";
    serviceMetadata?: BasicObjectMeta;
    storageClassName?: string;
}
export declare class Mongo {
    private metadata;
    private spec;
    constructor(metadata: ObjectMeta, spec: MongoSpec);
    get yaml(): string;
}
export {};
