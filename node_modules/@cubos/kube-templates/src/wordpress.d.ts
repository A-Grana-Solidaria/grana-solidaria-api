import { ObjectMeta } from "./kubernetes";
interface WordPressSpec {
    version: string;
    database: {
        host: string;
        username: string;
        password?: string;
        name?: string;
    };
    cpu: {
        request: string | number;
        limit: string | number;
    };
    memory: {
        request: string | number;
        limit: string | number;
    };
    ingress: {
        class?: "public" | "private" | "internal";
        publicUrl?: string;
        tlsCert?: string;
        timeout?: number;
        maxBodySize?: string;
    };
}
export declare class WordPress {
    private metadata;
    private spec;
    constructor(metadata: ObjectMeta, spec: WordPressSpec);
    get yaml(): string;
}
export {};
