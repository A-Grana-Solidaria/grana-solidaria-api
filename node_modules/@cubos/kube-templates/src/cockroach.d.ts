import { ObjectMeta } from "./kubernetes";
interface CockroachSpec {
    version: string;
    cpu: {
        request: string | number;
        limit: string | number;
    };
    memory: string | number;
    replicas: number;
}
export declare class Cockroach {
    private metadata;
    private spec;
    constructor(metadata: ObjectMeta, spec: CockroachSpec);
    get yaml(): string;
}
export {};
