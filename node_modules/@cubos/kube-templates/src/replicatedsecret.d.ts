import { ObjectMeta } from "./kubernetes";
interface ReplicatedSecretSpec {
    from: string;
}
export declare class ReplicatedSecret {
    private metadata;
    private spec;
    constructor(metadata: ObjectMeta, spec: ReplicatedSecretSpec);
    get yaml(): string;
}
export {};
