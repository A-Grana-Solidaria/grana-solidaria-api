import { Container, ObjectMeta } from "./kubernetes";
interface MariaDBSpec {
    version: string;
    cpu: {
        request: string | number;
        limit: string | number;
    };
    memory: string | number;
    rootPassword: string;
    initContainers?: Container[];
}
export declare class MariaDB {
    private metadata;
    private spec;
    constructor(metadata: ObjectMeta, spec: MariaDBSpec);
    get yaml(): string;
}
export {};
