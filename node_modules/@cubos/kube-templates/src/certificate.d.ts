import { ObjectMeta } from "./kubernetes";
interface CertificateSpec {
    domain: string;
    challengeType?: "http" | "dns";
    provider?: string;
    replicationAllowedNamespaces?: RegExp;
}
export declare class Certificate {
    private metadata;
    private spec;
    constructor(metadata: Omit<ObjectMeta, "name"> & {
        name?: string;
    }, spec: CertificateSpec);
    get yaml(): string;
}
export {};
