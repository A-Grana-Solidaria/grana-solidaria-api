import { ObjectMeta } from "./kubernetes";
interface StaticSiteSpec {
    provider?: "gcs" | "s3";
    publicUrl: string;
    ingressClass?: "public" | "private" | "internal";
    bucketName?: string;
    notFoundRedirect?: string;
    notFoundStatus?: number;
    tlsCert: string;
    additionalConfigurationSnippet?: string;
}
export declare class StaticSite {
    private metadata;
    private spec;
    constructor(metadata: ObjectMeta, spec: StaticSiteSpec);
    get yaml(): string;
}
export {};
